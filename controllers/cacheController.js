'use strict'

const User = require('../models/user');

function allUsers(req, res) {
    // Podria traerme todos los usuarios, guardarlos en caché y luego hacer que bull tome el mail de c/uno y les envíe un mensaje?
    let allUsersList = []

    
}